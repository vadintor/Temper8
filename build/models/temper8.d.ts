import { Sensor } from './sensor';
import { ReportParser } from './usb-sensor';
export declare class Temper8 implements ReportParser {
    private sensors;
    private nextSensor;
    private sensorDataListeners;
    private errorListeners;
    private unrecoverableError;
    initReport(): number[][];
    parseInput(data: number[]): number[];
    getSensorData(): Sensor[];
    addSensorDataListener(onSensorDataReceived: (sensor: Sensor[]) => void): void;
    addUnrecoverableErrorListener(onError: () => void): void;
    private updateSensorDataListeners(sensorData);
    private updateOnErrorListeners();
    private updateSensor(port, temperature);
    private connectSensors(total, used);
    private usedPortsRequest();
    private matchUsedPorts(data);
    private temperatureRequest(port);
    private matchTemperature(data);
    private GetTemperature(msb, lsb);
    private check03Request();
    private matchCheck03(data);
    private check05Request();
    private matchCheck05(data);
    private check0DRequest();
    private matchCheck0D(data);
    private checkFFRequest();
    private matchCheckFF(data);
}
